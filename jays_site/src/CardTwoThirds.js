import React from 'react';

export default class CardTwoThirds extends React.Component {

    render() {
        return (
            <div className="cardTwoThirds">
                <img className="" src={this.props.rawDataImg} alt="need to add the alt message in the raw data"/>
            </div>
        );
    }
}
